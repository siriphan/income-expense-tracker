{
  "name": "income-expense-github-cloudflare-final",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute income-expense-dashboard-db --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}